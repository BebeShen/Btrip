name: busan_trip
description: A simple Busan Trip planner app.

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^6.0.0
  geolocator: ^10.0.0
  image_picker: ^1.0.7
  sqflite: ^2.3.0
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
